name: Deploy RSS

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Upload RSS folder
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./rss

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
